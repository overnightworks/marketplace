{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/hooks/pre_commit_gate.sh\"",
            "timeout": 600,
            "statusMessage": "Atelier commit gate (nox -s agent)"
          }
        ]
      }
    ]
  }
}
